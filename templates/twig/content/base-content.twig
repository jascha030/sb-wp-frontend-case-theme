<div class="container mx-auto flex flex-col md:justify-center">
    <div class="text-center">
        <h1 class="text-8xl mt-32 md:-mt-32 mb-12 font-bold">
            Hey, listen...
        </h1>

        <div class="text-xl">
            <p>
                This is a base template, which should never be used in live context.<br>
                You can extend this template using the <a href="{{ twig_info.twig_repo_url }}" target="_blank">Twig-templating language</a> for PHP.
            </p>

            <p>
                You can read the full Twig documentation <a href="{{ twig_info.twig_docs_url }}" target="_blank">here</a>.
            </p>
        </div>
    </div>
</div>